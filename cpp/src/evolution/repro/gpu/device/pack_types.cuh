#pragma once

#include "gagp/evolution/repro/types.hpp"

namespace gagp::evo::repro {

using DPlainNode = PlainNode;
using DPackedProgramMeta = PackedProgramMeta;
using DCandidateRange = CandidateRange;

}  // namespace gagp::evo::repro
